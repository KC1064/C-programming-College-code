// WAP to find area of rectangle
#include<stdio.h>
void main(){
	int length, breadth;
	printf("Enter the length-:");
	scanf("%d", &length);
	printf("Enter the breadth-:");
	scanf("%d", &breadth);
	printf("The area of the reactangle is %d", length*breadth);
}
