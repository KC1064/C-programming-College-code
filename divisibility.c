#include <stdio.h>
void main(){
	int x;
	printf("Enter the number-:");
	scanf("%d",&x);
	if (x%7==0)
		printf("The number is divisible is 7\n");
	else
		printf("The number is not divisible is 7\n");	
}
