//WAP to find the larest of the two number
#include <stdio.h>
void main(){
	int a,b;
	printf("The two number are-:");
	scanf("%d %d",&a,&b);
	if (a>b)
		printf("The largest no. is %d\n",a);
	else
		printf("The largest no is %d\n",b);
}
